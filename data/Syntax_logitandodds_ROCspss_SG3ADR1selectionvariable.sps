* Encoding: UTF-8.

DATASET ACTIVATE DataSet1.

COMPUTE odds_SVM=EXP(PRED_1).
EXECUTE.

COMPUTE prob_SVM=odds_SVM / (1 + odds_SVM).
EXECUTE.

USE ALL.
COMPUTE filter_$=(SG = 3 &  ADR = 1).
VARIABLE LABELS filter_$ 'SG = 3 &  ADR = 1 (FILTER)'.
VALUE LABELS filter_$ 0 'Not Selected' 1 'Selected'.
FORMATS filter_$ (f1.0).
FILTER BY filter_$.
EXECUTE.

FREQUENCIES VARIABLES=drug prob_SVM
  /ORDER=ANALYSIS.

ROC ANALYSIS prob_SVM BY BIN_DILI (1)
  /MISSING USERMISSING=EXCLUDE
  /CRITERIA CUTOFF=INCLUDE TESTPOS=LARGE DISTRIBUTION=FREE CI=95
  /DESIGN PAIR=FALSE
  /PLOT CURVE=ROC MODELQUALITY=TRUE
  /PRINT SE=TRUE CLASSIFIER=TRUE.

ROC ANALYSIS prob_SVM BY BIN_DILI (1)
  /MISSING USERMISSING=EXCLUDE
  /CRITERIA CUTOFF=INCLUDE TESTPOS=LARGE DISTRIBUTION=FREE CI=95
  /DESIGN PAIR=FALSE
  /PLOT CURVE=ROC MODELQUALITY=TRUE
  /PRINT SE=TRUE CLASSIFIER=TRUE.

